version: '3.8'

services:
  server:
    build: .
    container_name: wandersphere
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: always